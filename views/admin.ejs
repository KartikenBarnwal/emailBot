<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body{
            background: #DC2424;  /* fallback for old browsers */
            background: -webkit-linear-gradient(to right, #4A569D, #DC2424);  /* Chrome 10-25, Safari 5.1-6 */
            background: linear-gradient(to right, #4A569D, #DC2424); /* W3C, IE 10+/ Edge, Firefox 16+, Chrome 26+, Opera 12+, Safari 7+ */

        }
        .admin{
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        
            display: flex;
            justify-content: center;
            width: 70vw;
            height: 80vh;            
            
            text-align: center;
        }
        #subject{
            text-align: center;
            height: 5vh;
        }
        input,textarea {
            /* outline: none; */
            padding: 5px;
            border: none;
            background: #fff;
            border-radius: 2px;
            display: inline-block;
            margin: .7rem;
            font-size: 1.1rem;
            position: relative;
            width: 50vw;
            text-align: center;
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
            box-shadow: 0 10px 20px rgba(0,0,0,0.19), 0 6px 6px rgba(0,0,0,0.23);
        }
        textarea{
            width: 60vw;
            /* height: 50%; */
        }
        #submit{
            width: 200px;
            background-color: black;
            color: whitesmoke;
        }
        #submit:hover{
            box-shadow: 0 14px 28px rgba(0,0,0,0.25), 0 10px 10px rgba(0,0,0,0.22);
            outline-offset: 3px;
        }
        
    </style>
</head>
<body>
    <div class="admin">
        <form action="/sendEmail" method="POST">
            <!-- <input type="text" id="sender" placeholder="Sending Name" name="sender" required> -->
            <input type="text" id="subject" placeholder="Subject" name="subject" required>

            <br>
            <textarea placeholder="Remember, be nice, be friendly! Go write a beautiful Email" cols="30" rows="5" name="content"></textarea>
            <textarea placeholder="Providing you the power of HTML, CSS & Bootstrap. Fill your creativity here!" cols="30" rows="5" name="htmlcontent"></textarea>
            <input type="submit" value="Send It!" id="submit">
        </form>
    </div>
</body>
</html>