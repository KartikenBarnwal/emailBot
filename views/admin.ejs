<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body{
            background-image: url('https://wallpapercave.com/wp/wp2570970.jpg');
            background-size: cover;
            background-attachment: fixed;
        }
        .admin{
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        
            display: flex;
            justify-content: center;
            width: 70vw;
            height: 75vh;
            background-color: rgba(255, 255, 255, 0.6);
            
            text-align: center;
        }
        #subject,#sender{
            text-align: center;
            height: 5vh;
        }
        input,textarea {
            /* outline: none; */
            padding: 5px;
            border: none;
            background: #fff;
            border-radius: 2px;
            display: inline-block;
            margin: .7rem;
            font-size: 1.1rem;
            position: relative;
            width: 50vw;
            text-align: center;
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
            box-shadow: 0 10px 20px rgba(0,0,0,0.19), 0 6px 6px rgba(0,0,0,0.23);
        }
        textarea{
            width: 60vw;
            height: 50%;
        }
        #submit{
            width: 200px;
            background-color: black;
            color: whitesmoke;
        }
        #submit:hover{
            box-shadow: 0 14px 28px rgba(0,0,0,0.25), 0 10px 10px rgba(0,0,0,0.22);
            outline-offset: 3px;
        }
        
    </style>
</head>
<body>
    <div class="admin">
        <form action="/sendEmail" method="POST">
            <!-- <input type="text" id="sender" placeholder="Sending Name" name="sender" required> -->
            <input type="text" id="subject" placeholder="Subject" name="subject" required>

            <br>
            <textarea placeholder="Remember, be nice, be friendly! Go write a beautiful Email" cols="30" rows="5" name="content"></textarea>
            <input type="submit" value="Send It!" id="submit">
        </form>
    </div>
</body>
</html>